server:
  port: 8006
spring:
  application:
    name: consul-provider-payment

  cloud:
    consul:
      host: localhost
      discovery:
        enabled: true
        service-name: ${spring.application.name}
      port: 8500
