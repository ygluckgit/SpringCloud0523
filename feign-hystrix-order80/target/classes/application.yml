spring:
  application:
    name: Feign-Hystrix-Order
server:
  port: 80
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
    fetch-registry: true
    register-with-eureka: true
feign:
  hystrix:
    enabled: true